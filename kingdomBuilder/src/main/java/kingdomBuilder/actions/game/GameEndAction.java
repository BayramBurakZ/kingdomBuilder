package kingdomBuilder.actions.game;

import kingdomBuilder.redux.Action;

/**
 * Represents the GameEndAction. Used for the {@link kingdomBuilder.redux.Store#dispatch(Action) dispatch()}-method
 * in the {@link kingdomBuilder.redux.Store Store} so the reducer knows what type of action he needs to run.
 */
public class GameEndAction extends Action {
}
